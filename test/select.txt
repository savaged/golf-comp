csvsql --query \
    "SELECT FORMAT(date_added, 'yyyy-MM-dd HH:mm') AS received, CAST(\`from\` AS INT) AS player, body FROM smshistory WHERE direction = 'in' AND body REGEXP '^[1-9]+ [1-9]+ [1-9]*$'" \
    smshistory.csv

